# 🚀 راهنمای سریع - Project Generator

## نصب و راه‌اندازی در 5 دقیقه

### گام 1: دانلود و آماده‌سازی

```bash
# کلون کردن پروژه
git clone <repository-url>
cd ProjectGenerator

# Build کردن
dotnet build
```

### گام 2: اجرای Windows Forms Application

```bash
cd ProjectGenerator.UI
dotnet run
```

### گام 3: تنظیمات در رابط گرافیکی

1. **تنظیمات پایه:**
   - نام پروژه: `MyShop`
   - مسیر خروجی: `C:\Projects` (یا مسیر دلخواه)
   - Namespace: `MyCompany.MyShop` (اختیاری)

2. **امکانات:**
   - ✅ شامل لایه WebSite
   - ✅ شامل پروژه Test
   - ✅ تولید داده اولیه
   - ✅ مدیریت کاربران
   - ✅ پنل فروشنده
   - ✅ کاتالوگ محصولات
   - ✅ سبد خرید
   - ✅ صدور فاکتور
   - ✅ سیستم بلاگ

3. **تنظیم نقش‌ها:**
   کلیک روی "تنظیم نقش‌ها" و نقش‌های زیر را اضافه کنید:
   - **Admin**: مدیر سیستم - تمام مجوزها
   - **Seller**: فروشنده - مدیریت محصولات و سفارشات
   - **User**: کاربر عادی - خرید و مشاهده

4. **تنظیم کاربران:**
   کلیک روی "تنظیم کاربران اولیه":
   - نام کاربری: `admin`
   - ایمیل: `admin@example.com`
   - شماره: `09123456789`
   - رمز: `Admin@123`
   - نقش: Admin

5. کلیک روی **"تولید پروژه"**

### گام 4: راه‌اندازی پروژه تولید شده

```bash
# رفتن به پوشه پروژه
cd C:\Projects\MyShop

# Restore پکیج‌ها
dotnet restore

# Build
dotnet build

# ایجاد دیتابیس
cd MyShop.WebSite
dotnet ef migrations add InitialCreate --project ../src/Infrastructure
dotnet ef database update

# اجرا
dotnet run
```

### گام 5: باز کردن در مرورگر

مراجعه به: `https://localhost:5001`

ورود با:
- نام کاربری: `admin`
- رمز عبور: `Admin@123`

## 🎯 دسترسی به پنل‌ها

بعد از ورود:

- **پنل مدیریت**: `/Admin`
- **پنل فروشنده**: `/Seller` (با حساب فروشنده)
- **پنل کاربری**: `/User`

## ⚡ دستورات مفید

### اجرای سریع با Console

```bash
cd ProjectGenerator
dotnet run -- -n MyShop -o C:\Projects --seed-data
```

### استفاده از فایل Config

```bash
dotnet run -- --config my-config.json
```

### دستورات دیتابیس

```bash
# ایجاد Migration
dotnet ef migrations add MigrationName --project ../src/Infrastructure

# اعمال Migration
dotnet ef database update

# حذف آخرین Migration
dotnet ef migrations remove --project ../src/Infrastructure
```

## 📁 ساختار پروژه تولید شده

```
MyShop/
├── MyShop.sln
├── src/
│   ├── Domain/              ← Entities
│   ├── SharedKernel/        ← Interfaces مشترک
│   ├── Application/         ← Business Logic
│   └── Infrastructure/      ← Data Access
├── MyShop.WebSite/         ← وب‌سایت
│   ├── Areas/
│   │   ├── Admin/          ← پنل مدیریت
│   │   ├── Seller/         ← پنل فروشنده
│   │   └── User/           ← پنل کاربری
│   ├── Controllers/
│   ├── Views/
│   └── wwwroot/
└── tests/
    └── UnitTests/
```

## 🔧 سفارشی‌سازی سریع

### تغییر رشته اتصال

فایل `appsettings.json` در `MyShop.WebSite`:

```json
{
  "ConnectionStrings": {
    "DefaultConnection": "Server=.;Database=MyShopDb;Trusted_Connection=True;..."
  }
}
```

### افزودن محصول جدید

1. برو به پنل مدیریت: `/Admin`
2. کلیک روی "محصولات"
3. "افزودن محصول جدید"

### ایجاد فروشنده جدید

1. پنل مدیریت → کاربران → افزودن
2. اطلاعات را وارد کن
3. نقش "Seller" را انتخاب کن

## ❓ مشکلات رایج

### خطای Migration

```bash
# پاک کردن دیتابیس و شروع دوباره
dotnet ef database drop --project ../src/Infrastructure --force
dotnet ef migrations add InitialCreate --project ../src/Infrastructure
dotnet ef database update
```

### خطای Build

```bash
# پاک کردن و Build مجدد
dotnet clean
dotnet build
```

### خطای Port

در `launchSettings.json` پورت را تغییر دهید.

## 📚 مراحل بعدی

1. ✅ پروژه را تولید کردی
2. ✅ دیتابیس را ایجاد کردی
3. ✅ با حساب Admin وارد شدی
4. 📝 محصول اول را اضافه کن
5. 🛒 یک سفارش تست ثبت کن
6. 📄 یک پست بلاگ بنویس
7. 🎨 طراحی را سفارشی کن

## 🆘 کمک نیاز داری؟

- 📖 README کامل: `README.md`
- 🐛 مشکل داری؟ Issue در GitHub
- 💡 ایده داری؟ Pull Request بفرست

---

**موفق باشی! 🚀**
