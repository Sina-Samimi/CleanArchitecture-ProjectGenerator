# 🚀 شروع سریع - Project Generator

## ✅ همه چیز آماده است!

### گام 1: انتخاب روش اجرا

#### 🖥️ Windows (با رابط گرافیکی)
```cmd
RUN_WINFORMS.bat
```

یا:
```cmd
cd ProjectGenerator.UI
dotnet run
```

#### 🐧 Linux / 🍎 Mac (Console)
```bash
./RUN_WINFORMS.sh
```

یا:
```bash
cd ProjectGenerator
dotnet run
```

---

## 📋 راهنمای استفاده (Windows Forms)

### گام 1: پر کردن تنظیمات پایه
```
نام پروژه: MyShop
مسیر خروجی: C:\Projects (یا هر مسیر دیگر)
Namespace: MyCompany.MyShop (اختیاری)
```

### گام 2: انتخاب امکانات
تیک تمام گزینه‌ها:
- ✅ شامل لایه WebSite
- ✅ شامل پروژه Test
- ✅ تولید داده اولیه
- ✅ مدیریت کاربران
- ✅ پنل فروشنده
- ✅ کاتالوگ محصولات
- ✅ سبد خرید
- ✅ صدور فاکتور
- ✅ سیستم بلاگ

### گام 3: تنظیم نقش‌ها (اختیاری ولی پیشنهادی)
```
کلیک "تنظیم نقش‌ها"
✅ نقش‌های پیش‌فرض نمایش داده می‌شوند:
   - Admin (مدیر سیستم)
   - Seller (فروشنده)
   - User (کاربر عادی)

می‌توانید:
- نقش جدید اضافه کنید
- نقش موجود را حذف کنید
- مجوزها را تنظیم کنید
```

### گام 4: تنظیم کاربران (اختیاری ولی پیشنهادی)
```
کلیک "تنظیم کاربران اولیه"
✅ کاربر پیش‌فرض نمایش داده می‌شود:
   - نام کاربری: admin
   - ایمیل: admin@example.com
   - رمز عبور: Admin@123
   - نقش: Admin

می‌توانید:
- کاربر جدید اضافه کنید
- کاربر موجود را حذف کنید
```

### گام 5: تولید پروژه
```
کلیک "تولید پروژه"
✅ نوار پیشرفت نمایش داده می‌شود
✅ پیغام موفقیت
✅ گزینه باز کردن پوشه
```

---

## 🎯 پس از تولید پروژه

### گام 1: رفتن به پوشه پروژه
```bash
cd C:\Projects\MyShop
```

### گام 2: Build
```bash
dotnet restore
dotnet build
```

### گام 3: ایجاد دیتابیس
```bash
cd MyShop.WebSite
dotnet ef migrations add InitialCreate --project ../src/Infrastructure
dotnet ef database update
```

### گام 4: اجرا
```bash
dotnet run
```

### گام 5: باز کردن در مرورگر
```
https://localhost:5001
```

### گام 6: ورود
```
نام کاربری: admin
رمز عبور: Admin@123
```

---

## ✨ دسترسی به پنل‌ها

بعد از ورود:

### پنل مدیریت
```
URL: https://localhost:5001/Admin
امکانات:
- مدیریت کاربران
- مدیریت نقش‌ها
- مدیریت محصولات
- مدیریت دسته‌بندی‌ها
- مدیریت سفارشات
- مدیریت بلاگ
```

### پنل فروشنده
```
URL: https://localhost:5001/Seller
امکانات:
- مدیریت محصولات خود
- مشاهده سفارشات
- داشبورد فروش
```

### پنل کاربری
```
URL: https://localhost:5001/User
امکانات:
- مدیریت پروفایل
- مشاهده سفارشات
- پیگیری فاکتورها
```

---

## 🐛 اگر مشکلی پیش آمد

### خطای Build
```bash
dotnet clean
dotnet restore
dotnet build
```

### خطای دیتابیس
```bash
cd MyShop.WebSite
dotnet ef database drop --force
dotnet ef migrations add InitialCreate --project ../src/Infrastructure
dotnet ef database update
```

### خطای Port
در `launchSettings.json` پورت را تغییر دهید.

---

## 📚 مستندات بیشتر

- `README_GENERATOR.md` - مستندات کامل
- `HOW_TO_RUN.md` - راهنمای اجرا
- `WINFORMS_FIX.md` - رفع مشکلات Windows Forms
- `ProjectGenerator/README.md` - راهنمای جزئیات
- `ProjectGenerator/QUICKSTART_FA.md` - راهنمای 5 دقیقه‌ای

---

## 💡 نکات مهم

1. **حتما نقش Admin داشته باشید**
2. **حداقل یک کاربر Admin تعریف کنید**
3. **Connection String را بررسی کنید**
4. **قبل از Production رمزها را تغییر دهید**

---

## 🎉 موفق باشید!

**اکنون می‌توانید پروژه‌های حرفه‌ای تولید کنید! 🚀**

سوال دارید؟ مستندات را بخوانید یا Issue در GitHub ایجاد کنید.
